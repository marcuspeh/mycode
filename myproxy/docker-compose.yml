version: "3.9"

services:
  myproxy:
    build: .
    container_name: myproxy
    restart: unless-stopped
    ports:
      - "3566:3566"
    environment:
      - MINIMAX_API_KEY=${MINIMAX_API_KEY:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    volumes:
      - ./config:/app/config
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3566/health"]
      interval: 30s
      timeout: 10s
      retries: 3
